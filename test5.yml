---
- hosts: wintest
  gather_facts: false
  vars:
  tasks:

  - name: test play
    win_ping:

  - name: ping serverA only
    debug:
      msg: "ping server A only"

  - name: ping serverB only
    debug:
      msg: "ping server B only"
