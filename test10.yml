---
- hosts: all
  gather_facts: true
  tasks:

  - name: First Playbook start
    debug:
      msg: "start of playbook - let's call the roll Playbook1 (test12)"

  - name: call the roll test12
    include_role:
      name: test12

  - name: The end
    debug:
      msg: "All done"
